import 'package:dart_learning/dart_lib.dart';
import 'package:test/test.dart';

void main() {
  test('calculate', () {
    expect(calculate(), 42);
    expect(calculate(), 42);
    expect(calculate(), 42);
    expect(calculate(), 42);
    expect(calculate(), 42);
    expect(calculate(), 42);
    expect(calculate(), 42);
    expect(calculate(), 42);
    expect(calculate(), 42);
  });
}
